services:
  - type: web
    name: garmin-tracker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app/web.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13